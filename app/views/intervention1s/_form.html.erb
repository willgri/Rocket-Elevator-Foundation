<%= form_with(model: intervention1, local: true) do |form| %>
  <% if intervention1.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(intervention1.errors.count, "error") %> prohibited this intervention1 from being saved:</h2>

      <ul>
      <% intervention1.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
