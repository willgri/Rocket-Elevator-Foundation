<%= form_with(model: google_map, local: true) do |form| %>
  <% if google_map.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(google_map.errors.count, "error") %> prohibited this google_map from being saved:</h2>

      <ul>
      <% google_map.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
